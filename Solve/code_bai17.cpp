#include <iostream>

#define MAX_NUM_DIGIT 9

using namespace std;

int main() {
    int num;
    cin >> num;

    for (int i = 1; i <= (1 << MAX_NUM_DIGIT); i++) {
        // See i in binary as a set of digits
        
    }
    return 0;
}